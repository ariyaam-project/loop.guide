{
  "name": "loops-guide",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "astro": "astro",
    "check": "astro check",
    "test": "tsx --test test/*.test.mjs",
    "test:api": "tsx --test test/should-i-loop-api.test.mjs",
    "test:classifier": "tsx --test test/classifier.test.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.2.1",
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.5"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "wrangler": "^3.99.0"
  }
}
